<% fields_for :asset_group do |f| %>
    <%= f.collection_select :asset_type_id, AssetType.all, :id, :name, {}, {:id => 'asset_types_dropdown_box'} %>  or <%= link_to "Add new.", {:controller => "asset_types", :action => "new", :asset_id => @asset, :referer => @referer}, :rel => 'facebox' %>
<% end %>