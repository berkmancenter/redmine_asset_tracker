<% fields_for :asset do |f| %>
     <%= f.collection_select :asset_group_id, AssetGroup.all, :id, :name, {:include_blank => 'NONE'}, {:id => 'asset_groups_dropdown_box'} %>  or <%= link_to l(:add_new), {:controller => "asset_groups", :action => "new", :asset_id => @asset}, :rel => 'facebox', :id => 'new_group_link' %>
<% end %>